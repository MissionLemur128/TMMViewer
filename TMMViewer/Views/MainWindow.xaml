<Window x:Class="TMMViewer.MainWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:views="clr-namespace:TMMViewer.Views"
        xmlns:viewModels="clr-namespace:TMMViewer.ViewModels"
        mc:Ignorable="d"
        Title="TMM Viewer" Height="450" Width="800">

    <Grid>
        <Grid.ColumnDefinitions>
            <ColumnDefinition Width="300"/>
            <ColumnDefinition Width="5"/>
            <ColumnDefinition Width="*"/>
        </Grid.ColumnDefinitions>

        <views:SideBar Grid.Column="0" HorizontalAlignment="Stretch" VerticalAlignment="Stretch"/>

        <GridSplitter Grid.Column="1" HorizontalAlignment="Stretch"/>

        <Grid Grid.Column="2">
            <views:MonoGameContentControl x:Name="Viewer3D"  DataContext="{Binding ModelViewer}" />

            <ComboBox ItemsSource="{Binding RenderModes}" SelectedItem="{Binding SelectedRenderMode}" Width="200" Height="24" HorizontalAlignment="Left" VerticalAlignment="Top" Margin="15"/>

        </Grid>
    </Grid>
</Window>
